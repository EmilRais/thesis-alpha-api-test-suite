# xit the error test for GET /board/get
# Write the empty list success test for GET /board/get
# Write the X boards success test for GET /board/get

# Only the npm test command will need to be used

#### pretest
# Creates a docker network || true
# Starts all external dependencies and waits

#### test-alpha-api-manual
# Starts emilrais/alpha-api-manual and waits
# Runs the entire test suite
# Stops emilrais/alpha-api-manual

#### test-alpha-api-generated
# Starts emilrais/alpha-api-generated and waits
# Runs the entire test suite
# Stops emilrais/alpha-api-generated

# if both tests go through then it is a great success
